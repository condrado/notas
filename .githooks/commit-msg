#!/bin/sh

set -e

commit_message=$(cat "$1")
case "$commit_message" in
  "chore: incrementar version"*) exit 0 ;;
esac

repo_root=$(git rev-parse --show-toplevel)
node "$repo_root/scripts/bump-version.mjs" "$commit_message"
git -C "$repo_root" add package.json README.md index.html